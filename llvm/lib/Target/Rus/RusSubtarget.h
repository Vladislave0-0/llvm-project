#ifndef LLVM_LIB_TARGET_RUS_RUSSUBTARGET_H
#define LLVM_LIB_TARGET_RUS_RUSSUBTARGET_H

#include "Rus.h"
#include "RusFrameLowering.h"
#include "RusISelLowering.h"
#include "RusInstrInfo.h"
#include "RusRegisterInfo.h"
#include "llvm/CodeGen/SelectionDAGTargetInfo.h"
#include "llvm/CodeGen/TargetSubtargetInfo.h"

#define GET_SUBTARGETINFO_HEADER
#include "RusGenSubtargetInfo.inc"

namespace llvm {

class RusSubtarget : public RusGenSubtargetInfo {
  RusTargetLowering TLInfo;
  RusFrameLowering FrameLowering;
  RusRegisterInfo RegInfo;
  RusInstrInfo InstrInfo;
  SelectionDAGTargetInfo TSInfo;

public:
  RusSubtarget(const Triple &TT, const std::string &CPU,
                  const std::string &FS, const TargetMachine &TM);

  /// ParseSubtargetFeatures - Parses features string setting specified
  /// subtarget options.  Definition of function is auto generated by tblgen.
  void ParseSubtargetFeatures(StringRef CPU, StringRef TuneCPU, StringRef FS);

  const RusTargetLowering *getTargetLowering() const override {
    RUS_DUMP_CYAN
    return &TLInfo;
  }
  const RusFrameLowering *getFrameLowering() const override {
    RUS_DUMP_CYAN
    return &FrameLowering;
  }

  const RusRegisterInfo *getRegisterInfo() const override {
    RUS_DUMP_CYAN
    return &RegInfo;
  }
  const RusInstrInfo *getInstrInfo() const override { return &InstrInfo; }
  const SelectionDAGTargetInfo *getSelectionDAGInfo() const override {
    RUS_DUMP_CYAN
    return &TSInfo;
  }
};

} // end namespace llvm

#endif // LLVM_LIB_TARGET_RUS_RUSSUBTARGET_H
